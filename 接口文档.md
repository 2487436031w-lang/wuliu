# 智慧物流平台 — 接口契约文档

> 本文档是前后端、软硬件三方解耦开发的唯一契约。三方开发时只认这份文档，互不阻塞。
> 技术栈：前端 Vue3 + Element Plus + 地图库；后端 Node.js(NestJS) + MQTT + InfluxDB/MySQL。

---

## 目录

1. [通用规范](#一通用规范)
2. [MQTT Topic 规范（设备 ↔ 后端）](#二mqtt-topic-规范设备--后端)
3. [REST API 清单（前端 ↔ 后端）](#三rest-api-清单前端--后端)
4. [WebSocket 推送规范（后端 → 前端）](#四websocket-推送规范后端--前端)
5. [数据库表设计](#五数据库表设计)

---

## 一、通用规范

### 1.1 REST 统一返回格式

所有 REST 接口统一返回如下结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 0 成功；非 0 失败（见错误码） |
| message | string | 提示信息 |
| data | object/array/null | 业务数据 |

### 1.2 错误码

| code | 含义 |
|------|------|
| 0 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录/登录过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突（如重复绑定） |
| 500 | 服务器内部错误 |

### 1.3 分页规范

列表接口统一使用分页参数，返回统一分页结构：

- 请求参数：`page`（页码，从 1 起）、`pageSize`（每页条数，默认 20）
- 返回结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

### 1.4 时间格式

所有时间字段统一使用 ISO 8601 字符串：`2026-08-21T16:05:00+08:00`（带时区）。

### 1.5 角色权限

| 角色 | 标识 | 权限范围 |
|------|------|---------|
| 货主 | shipper | 查看自己货物位置/轨迹/ETA、接收告警、智能问答 |
| 仓库管理员 | warehouse | 车辆管理、货物车辆绑定 |
| 调度员 | dispatcher | 车辆分布总览、调度指令下发、查看告警 |
| 司机 | driver | 货物状态上报、接收调度指令 |
| 系统管理员 | admin | 全部权限 + 告警日志管理 |

---

## 二、MQTT Topic 规范（设备 ↔ 后端）

### 2.1 Topic 命名

| 方向 | Topic | 说明 | QoS |
|------|-------|------|-----|
| 设备 → 后端 | `device/{deviceId}/gps` | GPS 位置上报 | 1 |
| 设备 → 后端 | `device/{deviceId}/alarm` | 异常告警上报 | 1 |
| 设备 → 后端 | `device/{deviceId}/status` | 设备心跳/在线状态 | 0 |
| 后端 → 设备 | `server/{deviceId}/dispatch` | 调度指令下发 | 1 |
| 后端 → 设备 | `server/{deviceId}/command` | 通用指令（备用） | 1 |

> `{deviceId}` 为设备唯一标识（与车辆绑定），示例：`device/ESP32-0001/gps`。

### 2.2 报文格式（均为 JSON）

**① GPS 位置上报 `device/{deviceId}/gps`**

```json
{
  "deviceId": "ESP32-0001",
  "timestamp": "2026-08-21T16:05:00+08:00",
  "longitude": 121.473701,
  "latitude": 31.230416,
  "speed": 62.5,
  "direction": 180,
  "altitude": 12.0
}
```

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| deviceId | string | - | 设备 ID |
| timestamp | string | - | 采集时间（ISO 8601） |
| longitude | float | 度 | 经度 |
| latitude | float | 度 | 纬度 |
| speed | float | km/h | 速度 |
| direction | float | 度 | 航向（0-360，正北为 0） |
| altitude | float | m | 海拔 |

**② 异常告警上报 `device/{deviceId}/alarm`**

```json
{
  "deviceId": "ESP32-0001",
  "timestamp": "2026-08-21T16:05:00+08:00",
  "alarmType": "off_route",
  "alarmLevel": 2,
  "description": "车辆偏离规划路线超过 500 米",
  "longitude": 121.473701,
  "latitude": 31.230416
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| alarmType | string | 告警类型，见下表 |
| alarmLevel | int | 级别 1=轻微 2=严重 3=紧急 |

`alarmType` 枚举：

| 值 | 含义 |
|------|------|
| off_route | 偏航 |
| abnormal_stop | 异常停留 |
| abnormal_open | 异常开箱 |

**③ 调度指令下发 `server/{deviceId}/dispatch`**

```json
{
  "commandId": "CMD-20260821-0001",
  "type": "reroute",
  "message": "请改道至 G2 高速，避开拥堵路段",
  "targetLongitude": 121.480000,
  "targetLatitude": 31.240000,
  "timestamp": "2026-08-21T16:05:00+08:00"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| commandId | string | 指令唯一 ID |
| type | string | 指令类型：reroute(改道)/stop(停车)/resume(继续) |
| message | string | 指令文字内容 |
| targetLongitude/Latitude | float | 目标点坐标（改道时） |

---

## 三、REST API 清单（前端 ↔ 后端）

### 3.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录，返回 token |
| POST | `/api/auth/logout` | 退出登录 |

### 3.2 车辆管理（仓库管理员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/vehicles` | 车辆列表（分页，可按状态筛选） |
| POST | `/api/vehicles` | 添加车辆 |
| GET | `/api/vehicles/{id}` | 车辆详情 |
| PUT | `/api/vehicles/{id}` | 更新车辆信息 |
| DELETE | `/api/vehicles/{id}` | 解绑/删除车辆 |

**添加车辆请求体示例：**

```json
{
  "plateNo": "沪A12345",
  "deviceId": "ESP32-0001",
  "type": "truck",
  "driverName": "张三",
  "driverPhone": "13800138000"
}
```

### 3.3 货物管理（货主）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/cargos` | 货物列表 |
| POST | `/api/cargos` | 创建货物 |
| GET | `/api/cargos/{id}` | 货物详情 |

### 3.4 货物车辆绑定（仓库管理员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/bindings` | 绑定关系列表 |
| POST | `/api/bindings` | 绑定货物与车辆 |
| DELETE | `/api/bindings/{id}` | 解绑 |

**绑定请求体示例：**

```json
{
  "cargoId": 101,
  "vehicleId": 201
}
```

### 3.5 位置与轨迹（货主/调度员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/vehicles/positions` | 所有车辆实时位置（分布总览） |
| GET | `/api/cargos/{id}/position` | 货物当前位置 |
| GET | `/api/cargos/{id}/track?start=&end=` | 历史轨迹（轨迹回放） |
| GET | `/api/cargos/{id}/eta` | 预计到达时间 ETA |

**轨迹返回示例：**

```json
{
  "cargoId": 101,
  "points": [
    { "timestamp": "2026-08-21T10:00:00+08:00", "longitude": 121.47, "latitude": 31.23, "speed": 60 }
  ]
}
```

**ETA 返回示例：**

```json
{
  "cargoId": 101,
  "eta": "2026-08-21T18:30:00+08:00",
  "remainingKm": 120.5,
  "remainingMinutes": 150
}
```

### 3.6 告警管理（调度员/管理员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/alarms` | 告警日志列表（分页，按状态/类型筛选） |
| GET | `/api/alarms/{id}` | 告警详情 |
| POST | `/api/alarms/{id}/resolve` | 关闭/解决告警 |

### 3.7 调度指令（调度员）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/dispatch` | 下发调度指令 |
| GET | `/api/dispatch?vehicleId=` | 指令历史 |

### 3.8 货物状态上报（司机）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/cargos/{id}/status` | 司机手动上报状态 |

**请求体示例：**

```json
{
  "status": "transporting"
}
```

`status` 枚举：`loaded`(已装货) / `transporting`(运输中) / `delivered`(已送达)

### 3.9 智能问答（货主）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat` | 发送问题，返回回答 |
| GET | `/api/chat/history` | 历史会话记录 |

**对话请求体示例：**

```json
{
  "question": "我的货物预计什么时候到？",
  "cargoId": 101
}
```

---

## 四、WebSocket 推送规范（后端 → 前端）

前端建立 WebSocket 连接后订阅实时数据，后端主动推送。

| 端点 | 推送内容 | 消费者 |
|------|---------|--------|
| `/ws/positions` | 车辆实时位置（GPS 坐标变化时） | 货主、调度员 |
| `/ws/alarms` | 实时告警通知 | 货主、调度员 |

### 4.1 位置推送报文

```json
{
  "type": "position",
  "data": {
    "vehicleId": 201,
    "cargoId": 101,
    "longitude": 121.473701,
    "latitude": 31.230416,
    "speed": 62.5,
    "timestamp": "2026-08-21T16:05:00+08:00"
  }
}
```

### 4.2 告警推送报文

```json
{
  "type": "alarm",
  "data": {
    "alarmId": 5001,
    "vehicleId": 201,
    "alarmType": "off_route",
    "alarmLevel": 2,
    "description": "车辆偏离规划路线",
    "timestamp": "2026-08-21T16:05:00+08:00"
  }
}
```

---

## 五、数据库表设计

### 5.1 关系型表（MySQL）

**vehicle 车辆表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| plate_no | VARCHAR(20) | 车牌号 |
| device_id | VARCHAR(64) | 设备 ID（MQTT 关联） |
| type | VARCHAR(20) | 车辆类型 |
| driver_name | VARCHAR(50) | 司机姓名 |
| driver_phone | VARCHAR(20) | 司机电话 |
| status | TINYINT | 0 离线 1 在线 |
| create_time | DATETIME | 创建时间 |

**cargo 货物表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| name | VARCHAR(100) | 货物名称 |
| owner_id | BIGINT | 货主用户 ID |
| status | VARCHAR(20) | loaded/transporting/delivered |
| create_time | DATETIME | 创建时间 |

**binding 绑定表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| cargo_id | BIGINT | 货物 ID |
| vehicle_id | BIGINT | 车辆 ID |
| bind_time | DATETIME | 绑定时间 |
| unbind_time | DATETIME | 解绑时间（可空） |

**alarm 告警表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| vehicle_id | BIGINT | 车辆 ID |
| cargo_id | BIGINT | 货物 ID（可空） |
| alarm_type | VARCHAR(30) | off_route/abnormal_stop/abnormal_open |
| alarm_level | TINYINT | 1/2/3 |
| description | VARCHAR(255) | 描述 |
| longitude | FLOAT | 告警位置经度 |
| latitude | FLOAT | 告警位置纬度 |
| status | TINYINT | 0 未解决 1 已解决 |
| create_time | DATETIME | 告警时间 |
| resolve_time | DATETIME | 解决时间（可空） |

**dispatch 指令表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| command_id | VARCHAR(64) | 指令唯一 ID |
| vehicle_id | BIGINT | 车辆 ID |
| type | VARCHAR(20) | reroute/stop/resume |
| message | VARCHAR(255) | 指令内容 |
| target_longitude | FLOAT | 目标经度（可空） |
| target_latitude | FLOAT | 目标纬度（可空） |
| status | TINYINT | 0 已下发 1 已执行 |
| create_time | DATETIME | 下发时间 |

**cargo_status_log 状态变更表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| cargo_id | BIGINT | 货物 ID |
| status | VARCHAR(20) | 状态 |
| operator | VARCHAR(50) | 操作人 |
| create_time | DATETIME | 变更时间 |

**user 用户表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| username | VARCHAR(50) | 用户名 |
| password | VARCHAR(255) | 密码（加密） |
| role | VARCHAR(20) | shipper/warehouse/dispatcher/driver/admin |

### 5.2 时序表（InfluxDB）

**gps_point 测量（measurement）**

| 字段 | 类型 | 说明 |
|------|------|------|
| device_id | tag | 设备 ID |
| longitude | field | 经度 |
| latitude | field | 纬度 |
| speed | field | 速度 km/h |
| direction | field | 航向 |
| altitude | field | 海拔 |
| timestamp | time | 采集时间 |

> 高频 GPS 坐标存 InfluxDB（按时间自动分片、支持聚合查询轨迹），业务数据存 MySQL。轨迹回放接口从 InfluxDB 按时间范围查询。

---

## 附：版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-21 | 初版，覆盖 11 项用户故事的接口、MQTT Topic、表结构 |
