# 发件箱 → 端侧：联调关注点

| 状态 | 待对方确认 |
| 日期 | 2026-08-22 |
| 发件 | 前端（代协作） |

## 前端不直连板子

位置与告警经 **后端 MQTT → REST/WS → 前端**。端侧请对齐后端冻结的 topic。

## MVP 端侧建议能力

1. `telemetry/gps` 周期上报（可用 sim）  
2. 开箱 event（杜邦）  
3. 收 dispatch/command → 蜂鸣/LED → ack  

样例起点：官方 `D13` GPS + `D5` MQTT。

## 请端侧确认

- deviceId 格式（是否 `ESP32-0001` 这类）  
- 室内 sim 与真 GPS 切换方式  
- 与 `接口文档.md` vs `docs/contracts/mqtt.md` 哪套 topic  

## 对方回复

（端侧填写）
