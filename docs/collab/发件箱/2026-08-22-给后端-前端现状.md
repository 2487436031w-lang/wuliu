# 发件箱 → 后端：前端现状与期望

| 状态 | 待对方确认 |
| 日期 | 2026-08-22 |
| 发件 | 前端 |

## 我们已有的

- 仓库路径：`web/`（Vue3），Mock 对齐根目录 `接口文档.md`。  
- 运行：`cd web && npm run dev` → http://localhost:5173  
- 登录：必须用户名+密码；**无注册页**（见协作区决策）。  
- 切换角色：退出后重新登录。

## 请后端提供 / 确认

1. 种子账号表（用户名、角色、是否固定密码）。  
2. CORS / JWT Header 名（文档写 token 还是 `Authorization: Bearer` —— 请统一一种）。  
3. MQTT topic 最终命名（与 `docs/contracts/mqtt.md` 二选一）。  
4. 本地联调地址（host、是否 Docker Compose）。  

## 我们暂不调用

- `POST /api/auth/register`  
- AI 相关进阶接口（等进阶 Sprint）  

回复请写在本文「对方回复」或新建 `收件箱/` 条目。

## 对方回复

（后端填写）
