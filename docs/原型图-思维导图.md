# 智慧路灯 — 原型图（页面思维导图）

> 按实际前端路由 `web/src/router/index.ts` 整理，对应「灯廊」低保真信息结构，不是视觉稿。  
> 预览本文件即可把思维导图当成原型站点地图。

## 1. 整站信息架构

```mermaid
mindmap
  root((灯廊原型))
    未登录
      登录页 /login
        用户名密码
        注册入口
        成功后进总览
    已登录壳 AppShell
      侧栏导航
      顶栏角色与退出
      总览 /dashboard
        设备数量与在线统计
        实时光照卡片
      设备管理 /devices
        设备列表 ON OFF
        在线灰显 OFFLINE
        新增编辑删除
        手动开关按钮
      光照监测 /lights
        光照记录列表
        趋势折线
        按设备筛选
      告警管理 /alarms
        告警列表 ACTIVE
        标记已解决
        告警统计
      阈值配置 /threshold
        仅 ADMIN 可进
        开灯阈值
        关灯阈值
        心跳超时秒数
      控制日志 /logs
        自动手动来源
        成功失败
```

## 2. 页面跳转原型

```mermaid
flowchart LR
  Login["登录 /login"] --> Dash["总览 /dashboard"]
  Dash --> Dev["设备 /devices"]
  Dash --> Light["光照 /lights"]
  Dash --> Alarm["告警 /alarms"]
  Dash --> Log["日志 /logs"]
  Dash -.->|"仅 ADMIN"| Th["阈值 /threshold"]
  Dev --> Dash
  Staff["市政人员"] --> Dash
  Admin["管理员"] --> Th
```

未登录访问业务页 → 拉回 `/login`。市政人员点阈值 → 退回总览。

## 3. 各页原型要点（线框级）

### 登录 `/login`

- 表单：用户名、密码  
- 操作：登录、注册  
- 无侧栏  

### 总览 `/dashboard`

```text
+------------------+------------------+
| 设备总数 / 在线   | 告警活跃数        |
+------------------+------------------+
| 最新光照（实时刷新，Mock 或 WS）      |
+-------------------------------------+
```

### 设备 `/devices`

```text
[新增设备]  筛选：名称 / 开关 / 在线
--------------------------------
名称 | SN | 开关 | 在线 | 心跳时间 | 开/关 | 编辑 | 删
```

### 光照 `/lights`

```text
设备筛选 | 时间范围
列表：时间、设备、光照值
下方：趋势图
```

### 告警 `/alarms`

```text
类型 OFFLINE 等 | 状态 ACTIVE/RESOLVED
行操作：解决
```

### 阈值 `/threshold`（管理员）

```text
开灯阈值 lightThresholdOn
关灯阈值 lightThresholdOff
心跳超时 heartbeatTimeout
[保存]
```

### 日志 `/logs`

```text
时间 | 设备 | AUTO/MANUAL | ON/OFF | 结果
```

## 4. 角色 × 页面

```mermaid
flowchart TB
  subgraph staff["市政人员"]
    S1[总览]
    S2[设备与手动开关]
    S3[光照]
    S4[告警]
    S5[日志]
  end
  subgraph admin["管理员另加"]
    A1[阈值配置]
    A2[同上全部页面]
  end
```

默认 Mock 账号见 `web/README.md`：`admin` / `admin123`，`staff` / `staff123`。
